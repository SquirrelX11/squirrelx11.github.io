Drop B-DAY assets here.

- icon.png       → app icon (used by the Explore card; falls back to a glass tile if missing)
- hero.webp      → optional final hero artwork (see .hero-visual swap point in app/globals.css)

Until real assets are added, the UI shows calm placeholders — no fake mockups.
